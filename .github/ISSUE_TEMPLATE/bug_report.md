---
name: Bug report
about: Something isn't reconciling the way you expect
labels: bug
---

**What happened**

<!-- Include the relevant log lines from the Lambda run (JSON logs). Redact emails/ids. -->

**What you expected**

**Configuration**

- Version (SAR/tag):
- `MANAGED_GROUP_PREFIX`:
- `PRUNE_USERS`:
- `DRY_RUN`:
- `STATE_BUCKET` set? (stateful vs natural-key matching):
- `GOOGLE_GROUPS_QUERY`:

**Anything else**
